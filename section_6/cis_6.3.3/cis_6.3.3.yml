---

{{ if .Vars.suse16cis_level_2 }}
  {{ if .Vars.suse16cis_rule_6_3_3 }}
file:
  /etc/aide/aide.conf:
    title: 6.3.3 | Ensure cryptographic mechanisms are used to protect the integrity of audit tools
    exists: true
    contents:
    - '/usr/sbin/auditctl'
    - '/usr/sbin/auditd'
    - '/usr/sbin/augenrules'
    - '/usr/sbin/aureport'
    - '/usr/sbin/ausearch'
    - '/usr/sbin/autrace'
    meta:
      server: 2
      workstation: 2
      CIS_ID:
      - 6.3.3
  {{ end }}
{{ end }}
