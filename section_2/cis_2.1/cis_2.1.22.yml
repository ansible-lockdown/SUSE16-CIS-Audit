---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_2_1_22 }}
command:
  approved_listening_services_manual:
    title: 2.1.22 | Ensure only approved services are listening on a network interface
    exec: echo "Manual check - review the output of 'ss -plntu' and confirm every listening service is approved for this host"
    exit-status: 0
    stdout: []
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.1.22
      NIST800-53R5:
      - CM-7
  {{ end }}
{{ end }}
