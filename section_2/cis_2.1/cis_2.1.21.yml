---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_2_1_21 }}
    {{ if not .Vars.suse16cis_xwindow_server }}
package:
  xwindow_pkg:
    title: 2.1.21 | Ensure mail transfer agents are configured for local-only mode | pkg removed
    name: xorg-x11-server
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.1.21
      NIST800-53R5:
      - CM-11
    {{ end }}
  {{ end }}
{{ end }}
