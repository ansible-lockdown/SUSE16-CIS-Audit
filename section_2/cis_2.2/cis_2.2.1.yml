---

{{ if not .Vars.suse16cis_ftp_client }}
  {{ if .Vars.suse16cis_level_1 }}
    {{ if .Vars.suse16cis_rule_2_2_1 }}
package:
  ftp_client:
    title: 2.2.1 | Ensure ftp client is not installed
    installed: false
    name: ftp
    meta:
      server: 1
      workstation: 1
      CIS_ID: 2.2.1
      NIST800-53R5:
      - CM-7
      - CM-11
    {{ end }}
  {{ end }}
{{ end }}
