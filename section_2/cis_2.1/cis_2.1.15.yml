---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_2_1_15 }}
    {{ if not .Vars.suse16cis_telnet_server }}
      {{ if not .Vars.suse16cis_telnet_mask }}
package:
  telnet_pkg:
    title: 2.1.15 | Ensure telnet server services are not in use | pkg removed
    name: telnet-server
    installed: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.1.15
      NIST800-53R5:
      - CM-7
      {{ end }}
      {{ if .Vars.suse16cis_telnet_mask }}
file:
  telnet_socket_masked:
    title: 2.1.15 | Ensure telnet server services are not in use | masked
    path: /etc/systemd/system/telnet.socket
    exists: true
    filetype: symlink
    linked-to: /dev/null
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.1.15
      NIST800-53R5:
      - CM-7
      {{ end }}
    {{ end }}
  {{ end }}
{{ end }}
