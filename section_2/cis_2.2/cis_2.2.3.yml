---

{{ if not .Vars.suse16cis_nis_client_required }}
  {{ if .Vars.suse16cis_level_1 }}
    {{ if .Vars.suse16cis_rule_2_2_3 }}
package:
  nis_client:
    title: 2.2.3 | Ensure nis client is not installed
    installed: false
    name: ypbind
    meta:
      server: 1
      workstation: 1
      CIS_ID: 2.2.3
      NIST800-53R5:
      - CM-7
      - CM-11
    {{ end }}
  {{ end }}
{{ end }}
