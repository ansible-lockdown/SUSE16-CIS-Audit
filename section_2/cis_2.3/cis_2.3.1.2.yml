---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_2_3_1_2 }}
service:
  {{ if eq .Vars.suse16cis_time_sync_tool "chronyd" }}
  chronyd:
    title: 2.3.1.2 | Ensure chrony is enabled and running | chronyd
    running: true
    enabled: true
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.3.1.2
      NIST800-53R5:
      - AU-8
  {{ end }}
  {{ if eq .Vars.suse16cis_time_sync_tool "systemd-timesyncd" }}
  systemd-timesyncd:
    title: 2.3.1.2 | Ensure chrony is enabled and running | systemd-timesyncd
    running: true
    enabled: true
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.3.1.2
      NIST800-53R5:
      - AU-8
  {{ end }}
  {{ end }}
{{ end }}
