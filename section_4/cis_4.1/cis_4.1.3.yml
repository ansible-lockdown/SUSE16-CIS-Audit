---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_4_1_3 }}
    {{ if eq .Vars.suse16cis_firewall "firewalld" }}
service:
  firewalld:
    title: 4.1.3 | Ensure firewalld.service is configured
    enabled: true
    running: true
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 4.1.3
      NIST800-53R5:
      - NA
    {{ end }}
  {{ end }}
{{ end }}
