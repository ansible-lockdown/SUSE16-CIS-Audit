---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_1_1_2_1_2 }}
mount:
  tmp_nodev_options:
    title: 1.1.2.1.2 | Ensure nodev option set on /tmp partition
    mountpoint: /tmp
    exists: true
    opts:
    - nodev
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 1.1.2.1.2
      NIST800-53R5:
      - CM-7
      - AC-3
      - MP-2
command:
  tmp_nodev_persistent_options:
    title: 1.1.2.1.2 | Ensure nodev option set on /tmp partition | persistent config
    exec: |
      if [ "$(systemctl show tmp.mount -p SourcePath --value)" = "/etc/fstab" ]; then
        findmnt --fstab -n -o OPTIONS /tmp
      else
        systemctl cat tmp.mount 2>/dev/null | awk -F= '/^Options=/{print $2}'
      fi
    exit-status: 0
    stdout:
    - '/\bnodev\b/'
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 1.1.2.1.2
      NIST800-53R5:
      - CM-7
      - AC-3
      - MP-2
  {{ end }}
{{ end }}
