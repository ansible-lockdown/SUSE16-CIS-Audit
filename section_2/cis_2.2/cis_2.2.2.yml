---

{{ if not .Vars.suse16cis_ldap_clients_required }}
  {{ if .Vars.suse16cis_level_2 }}
    {{ if .Vars.suse16cis_rule_2_2_2 }}
package:
  {{- range mkSlice "openldap2_6-client" }}
  {{ . }}_removed:
    title: 2.2.2 | Ensure ldap client is not installed | {{ . }} package
    installed: false
    name: {{ . }}
    meta:
      server: 2
      workstation: 2
      CIS_ID: 2.2.2
      NIST800-53R5:
      - CM-7
      {{ end }}
    {{ end }}
  {{ end }}
{{ end }}
