---

{{ if .Vars.suse16cis_level_1 }}
  {{ if .Vars.suse16cis_rule_2_4_3_1 }}
command:
  timer_unit_perms:
    title: 2.4.3.1 | Ensure access to systemd timer unit files is configured
    exit-status:
      or:
      - 0
      - 1
    exec: find -type f /run/systemd/system /etc/systemd/system /usr/lib/systemd/system -name '*.timer' \( ! -user root -o ! -group root -o -perm /133 \) 2>/dev/null
    stdout:
    - '!/^.*/'
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 2.4.3.1
      NIST800-53R5:
      - CM-6
      - CM-5
      - AC-6
  {{ end }}
{{ end }}
