---

{{ if .Vars.suse16cis_level_2 }}
  {{ if .Vars.suse16cis_rule_6_2_4_9 }}
file:
  {{- range mkSlice "/sbin/auditctl" "/sbin/aureport" "/sbin/ausearch" "/sbin/auditd" "/sbin/augenrules"}}
  {{ . }}_mode:
    title: 6.2.4.9 | Ensure audit tools owner is configured
    path: {{ . }}
    exists: true
    owner: root
    meta:
      server: 2
      workstation: 2
      CIS_ID:
      - 6.2.4.9
      NIST800-53R5:
      - AU-9
    {{ end }}
  {{ end }}
{{ end }}
